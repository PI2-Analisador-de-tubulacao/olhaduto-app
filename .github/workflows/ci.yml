name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - name: Build and Run
        run: |
          docker-compose run -u root olhaduto-desktop chown olhaduto:olhaduto . -R
          docker-compose up -d

      - name: Show status
        run: |
          ls -la
          docker-compose ps -a

      - name: Run checkings
        run: |
          docker-compose exec -T olhaduto-desktop yarn prestart
          docker-compose exec -T olhaduto-desktop yarn test
          docker-compose exec -T olhaduto-desktop yarn lint
          docker-compose exec -T olhaduto-desktop yarn lint-styles
